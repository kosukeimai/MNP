language: r
r:
  - release
  - devel

cache: packages
sudo: required

script:
    - Rscript -e 'library(MNP); data(detergent)'
    - Rscript test.R

notifications:
  email:
    on_success: change
    on_failure: change
